import React from "react";
import { LandingContainer } from "./landing-css";

const Landing = (props: any) => {
  return <LandingContainer className=" h-screen w-screen"></LandingContainer>;
};

export default Landing;
